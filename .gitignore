node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Segredos — nunca commitar (RESEND_API_KEY, etc.)
.env
.env.local
.env.*.local

.vercel
*.log
.DS_Store

# Ficheiros internos do Claude Code
.claude/

# Instrucoes pessoais do projeto - nao deve ficar exposto no repositorio
CLAUDE.MD
